<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nouf.projects.clothingtermsdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".videoPlayer" />
        <activity android:name=".terms_list_mother"></activity>
        <activity android:name=".terms_list" />
        <activity android:name=".details2" />
        <activity android:name=".Main2Activity">
        </activity>
        <activity android:name=".launchingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".machine_list" />
        <activity android:name=".MotherList" />
        <activity android:name=".MainActivity" />
        <activity android:name=".details" />
        <activity
            android:name=".inside_category"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>