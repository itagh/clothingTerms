<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nouf.projects.clothingtermsdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".details" />
        <activity android:name=".categoryActivity_noNeed" />
        <activity android:name=".launchingActivity" />
        <activity android:name=".MainActivity_noNeed" />
        <activity android:name=".inside_category"
        android:parentActivityName=".Main2Activity"/>
    </application>

</manifest>